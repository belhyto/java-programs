
/**
 * Write a description of class magicSq here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
import java.util.scanner;
public class magicSq
{
    // instance variables - replace the example below with your own
    private int x;
    int [][]mat;
    int d1sum, d2sum, rsum, csum;

   /**
     * Constructor for objects of class magicSq
     */
    public magicSq(int row, int column )
    {
        // initialise instance variables
        mat= new int[row][column];
         }

    /**
     * An example of a method - replace this comment with your own
     *
     * @param  y  a sample parameter for a method
     * @return    the sum of x and y
     */
    public boolean isMagic(int y)
    {
        // put your code here
        return x + y;
    }
}
